services:
  api:
    build: .
    container_name: lyceum-api
    restart: unless-stopped
    ports: ["127.0.0.1:4700:4700"]
    env_file: .env
    volumes: ["./data:/data"]
    logging: { driver: json-file, options: { max-size: "10m", max-file: "5" } }
